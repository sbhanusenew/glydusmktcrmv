{
  "name": "glydus-crm-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Glydus CRM demo with admin and user portals.",
  "scripts": {
    "dev": "node serve-glydus-crm.js",
    "start": "node serve-glydus-crm.js",
    "check": "node --check glydus-crm.js && node --check serve-glydus-crm.js && node --check api/config.js && node --check api/admin-users.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
